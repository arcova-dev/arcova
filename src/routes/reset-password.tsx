import {createFileRoute,useNavigate} from "@tanstack/react-router";import{useState}from"react";import{supabase}from"@/integrations/supabase/client";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{meta}from"@/components/page-kit";export const Route=createFileRoute("/reset-password")({head:()=>meta("Reset password","Set a new ARCOVA account password from a secure recovery link."),component:Page});function Page(){const nav=useNavigate();const[p,setP]=useState("");const[m,setM]=useState("");async function go(e:React.FormEvent){e.preventDefault();if(!window.location.hash.includes("type=recovery")){setM("Open the recovery link from your email.");return}const{error}=await supabase.auth.updateUser({password:p});if(error){setM(error.message);return}await nav({to:"/agent"})}return <section className="auth-page"><form className="auth-panel" onSubmit={go}><span className="eyebrow">ACCOUNT RECOVERY</span><h1>Set new password</h1><label>NEW PASSWORD<Input type="password" minLength={8} value={p} onChange={e=>setP(e.target.value)} required/></label><Button>UPDATE PASSWORD</Button>{m&&<p>{m}</p>}</form></section>}
