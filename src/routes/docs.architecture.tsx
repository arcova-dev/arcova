import{createFileRoute}from"@tanstack/react-router";import{meta}from"@/components/page-kit";export const Route=createFileRoute("/docs/architecture")({head:()=>meta("Architecture","How ARCOVA separates data, intelligence, permission and settlement layers."),component:Page});function Page(){return <><span className="eyebrow">DOCUMENTATION</span><h1>Architecture</h1><p className="lead">How ARCOVA separates data, intelligence, permission and settlement layers.</p><h2>Overview</h2><p>Provider interfaces isolate market, fundamental, news, registry, network and onchain sources. Missing providers resolve to explicit unavailable states instead of substitute values.</p><div className="doc-note"><strong>DATA INTEGRITY</strong><p>Values, claims and transaction records are displayed only when backed by a connected source.</p></div></>}