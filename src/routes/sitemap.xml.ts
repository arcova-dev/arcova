import { createFileRoute } from "@tanstack/react-router";
const paths = ["/", "/agent", "/markets", "/terminal", "/scanner", "/compare", "/portfolio", "/watchlist", "/intelligence", "/reports", "/activity", "/agent/performance", "/agent/settings", "/token", "/docs", "/docs/architecture", "/docs/agent", "/docs/tokenized-markets", "/docs/execution", "/docs/risk", "/about", "/roadmap", "/faq"];
export const Route = createFileRoute("/sitemap.xml")({ server: { handlers: { GET: ({ request }) => { const origin = new URL(request.url).origin; const xml = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(path => `<url><loc>${origin}${path}</loc></url>`).join("")}</urlset>`; return new Response(xml, { headers: { "content-type": "application/xml" } }); } } } });