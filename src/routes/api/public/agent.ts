import{createFileRoute}from"@tanstack/react-router";import{createClient}from"@supabase/supabase-js";import{z}from"zod";import type{Database,Json}from"@/integrations/supabase/types";const schema=z.object({threadId:z.string().uuid(),message:z.string().trim().min(1).max(4000)});export const Route=createFileRoute("/api/public/agent")({server:{handlers:{POST:async({request})=>{const auth=request.headers.get("authorization")??"";if(!auth.startsWith("Bearer "))return new Response("Unauthorized",{status:401});const url=process.env['SUPABASE_URL'];const key=process.env['SUPABASE_PUBLISHABLE_KEY'];if(!url||!key)return new Response("Service unavailable",{status:503});const db=createClient<Database>(url,key,{global:{headers:{Authorization:auth}},auth:{persistSession:false}});const{data:{user}}=await db.auth.getUser(auth.slice(7));if(!user)return new Response("Unauthorized",{status:401});const parsed=schema.safeParse(await request.json());if(!parsed.success)return new Response("Invalid request",{status:400});const{threadId,message}=parsed.data;const{data:thread}=await db.from("agent_threads").select("id").eq("id",threadId).eq("user_id",user.id).maybeSingle();if(!thread)return new Response("Thread not found",{status:404});const answer="Market and news providers are not currently configured. I cannot produce a responsible asset analysis, scenario probability, price view, liquidity assessment, or recommendation without validated source data. Connect a verified provider and retry; I will then show sources, timestamps, assumptions, confidence, risks, catalysts, and invalidation conditions.";const now=new Date().toISOString();const userRow={id:crypto.randomUUID(),thread_id:threadId,user_id:user.id,role:"user",parts:[{type:"text",text:message}] as Json,created_at:now,ai_message_id:null};const assistantRow={id:crypto.randomUUID(),thread_id:threadId,user_id:user.id,role:"assistant",parts:[{type:"text",text:answer}] as Json,created_at:new Date(Date.now()+1).toISOString(),ai_message_id:null};const{error}=await db.from("agent_messages").insert([userRow,assistantRow]);if(error)return new Response("Unable to save response",{status:500});await db.from("agent_threads").update({updated_at:new Date().toISOString()}).eq("id",threadId);return Response.json({user:userRow,assistant:assistantRow})}}}});
