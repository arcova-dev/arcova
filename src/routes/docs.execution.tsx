import{createFileRoute}from"@tanstack/react-router";import{meta}from"@/components/page-kit";export const Route=createFileRoute("/docs/execution")({head:()=>meta("Execution","ARCOVA’s policy-gated transaction lifecycle."),component:Page});function Page(){return <><span className="eyebrow">DOCUMENTATION</span><h1>Execution</h1><p className="lead">ARCOVA’s policy-gated transaction lifecycle.</p><h2>Overview</h2><p>USER POLICY → AGENT DECISION → RISK CHECK → PERMISSION CHECK → USER APPROVAL → TRANSACTION → ONCHAIN CONFIRMATION. ARCOVA never requests private keys or seed phrases.</p><div className="doc-note"><strong>DATA INTEGRITY</strong><p>Values, claims and transaction records are displayed only when backed by a connected source.</p></div></>}