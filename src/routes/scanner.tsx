import {createFileRoute} from "@tanstack/react-router"; import {FeaturePage} from "@/components/feature-page"; import {meta} from "@/components/page-kit"; export const Route=createFileRoute("/scanner")({head:()=>meta("Market Scanner","ARCOVA market scanner with source-aware data states and transparent analysis."),component:()=> <FeaturePage kind="scanner"/>});