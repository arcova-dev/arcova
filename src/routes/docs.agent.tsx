import{createFileRoute}from"@tanstack/react-router";import{meta}from"@/components/page-kit";export const Route=createFileRoute("/docs/agent")({head:()=>meta("Agent System","How ARCOVA produces bounded, explainable model views."),component:Page});function Page(){return <><span className="eyebrow">DOCUMENTATION</span><h1>Agent System</h1><p className="lead">How ARCOVA produces bounded, explainable model views.</p><h2>Overview</h2><p>The agent combines validated inputs into bull, base and bear scenarios, then exposes confidence, assumptions, catalysts, key risks and invalidation conditions.</p><div className="doc-note"><strong>DATA INTEGRITY</strong><p>Values, claims and transaction records are displayed only when backed by a connected source.</p></div></>}