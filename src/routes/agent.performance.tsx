import{createFileRoute}from"@tanstack/react-router";import{BarChart3}from"lucide-react";import{DataPanel,Metric,PageHero,meta}from"@/components/page-kit";export const Route=createFileRoute("/agent/performance")({head:()=>meta("Agent Performance","Transparent ARCOVA decision, scenario and execution methodology."),component:Page});function Page(){return <><PageHero eyebrow="MODEL ACCOUNTABILITY" title="Agent performance" description="Track decisions and outcomes without cherry-picking, hindsight rewriting or conflated metrics." icon={BarChart3}/><section className="section-wrap"><div className="data-grid">{["DECISIONS","EXECUTED TRADES","OPEN POSITIONS","CLOSED POSITIONS","AVG HOLDING TIME","DIRECTION ACCURACY","SCENARIO ACCURACY","EXECUTION SUCCESS"].map(x=><Metric key={x} label={x}/>)}</div><DataPanel title="METHODOLOGY"><p>Direction accuracy measures whether the model direction matched the defined outcome window. Scenario accuracy measures whether observed conditions remained within a stated scenario. Execution success measures authorized transactions confirmed as intended. Results require complete, source-backed records.</p></DataPanel></section></>}
